<%= form_for(@meal_plan) do |f| %>
  <% if @meal_plan.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meal_plan.errors.count, "error") %> prohibited this meal_plan from being saved:</h2>

      <ul>
      <% @meal_plan.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :starting_at %>
    <%= date_select :meal_plan, :starting_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
